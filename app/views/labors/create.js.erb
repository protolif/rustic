$("labor_table").insert(
  {'bottom' : "<%= escape_javascript(render('labor', :labor => @labor)) %>"}
);
$("new_labor").reset();
$("bill").update("<%= escape_javascript(render('tickets/bill', :ticket => @labor.ticket)) %>");
