<fieldset>
	<legend>Computer</legend>
	<%= link_to("#{pc.make} #{pc.model}
	    #{truncate(pc.serial, :length => 20)}", computer_path(pc)) %>
	<br />
	<%= pc.form_factor %>
	<%= pc.cpu %>
	<%= pc.ram %><br />
	<%= charger_icon if pc.charger %>
</fieldset>