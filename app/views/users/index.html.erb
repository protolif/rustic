<h1>All users</h1>

<%= form_tag(users_path, :method => "get") do %>
  <%= label_tag(:search, "Last Name:") %>
  <%= text_field_tag(:search) %>
  <%= submit_tag("Search") %>
  <%= link_to("[Back]", users_path) %>
<% end %>

<ul class="users">
  <%= render @users %>
</ul>

<%= will_paginate %>